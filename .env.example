# Starknet Remote Signer Configuration

# Keystore Backend Configuration
SIGNER_KEYSTORE_BACKEND=environment  # "software", "environment", "hsm"

# For Environment Backend (Development)
SIGNER_PRIVATE_KEY=your_private_key_here

# For Software Backend (Production)
# SIGNER_KEYSTORE_PATH=/path/to/keystore.json
# SIGNER_PASSPHRASE=your_secure_passphrase

# API key for authentication (highly recommended for production)
SIGNER_API_KEY=your_secure_api_key_here

# Server configuration
SIGNER_ADDRESS=127.0.0.1
SIGNER_PORT=3000

# Logging level (trace, debug, info, warn, error)
RUST_LOG=info

# TLS configuration (not yet implemented)
# SIGNER_TLS=true
# SIGNER_TLS_CERT=/path/to/cert.pem
# SIGNER_TLS_KEY=/path/to/key.pem 